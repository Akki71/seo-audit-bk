exports.generatePdfLogic = async ({ domainId }) => {
  // 🔁 MOVE logic from generatePdf controller here// crawl → GA → GSC → AI → HTML → PDF → saveconsole.log(`PDF generated for domain ID ${domainId}`);
};
 